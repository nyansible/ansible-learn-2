all:
  children:
    vpn:
      hosts:
        localhost:
          ansible_connection: local
          ansible_become: true
          wireguard_addresses:
            - "10.8.0.1/24"
          wireguard_endpoint: ""
          wireguard_port: 51820
          wireguard_persistent_keepalive: 25
          #weirdly this is pushed to the far end
          #wireguard_allowed_ips: "0.0.0.0/0"
          
